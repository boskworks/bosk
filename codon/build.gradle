
dependencies {
	// BytecodeDisassembler exposes org.slf4j.Logger in its API, so this must be `api`
	// to match the `requires transitive` in module-info.java.
	api libs.slf4j.api
	// jspecify for annotations, and the like, come from the root build.gradle.
}
