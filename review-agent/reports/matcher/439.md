# Matcher validation, PR 439

Source review: `review-agent/data/eval/5fc3871d97-deepseek-deepseek-v4-flash-3c2cfb106c/439.json`. Matcher verdicts: cached (`review-agent/data/eval/5fc3871d97-deepseek-deepseek-v4-flash-3c2cfb106c/439.match.json`).

The matcher decides which review comments capture each gold finding; recall is measured from these verdicts. For each row, record in the `Your call` column whether the matcher's verdict is right: `correct` or `wrong`, plus an optional note. For a `missed` verdict, scan the review comments below to confirm none captures the finding. Recall is not trusted until these are checked.

Captured 0/4 findings.

| # | Your finding | Reviewer comment | Matcher verdict | Your call |
|---|---|---|---|---|
| 0 | I'd add a javadoc saying this is equivalent to `_skipToEnd(0)` but faster. | — | missed | correct |
| 1 | This reminds me: `doStateTransition` needs its javadoc to describe what the argument means. | — | missed | correct |
| 2 | Needs a javadoc. | — | missed | correct |
| 3 | The comments shouldn't be a history lesson. Just describe what the code does, not what it used to do. | — | missed | correct |

## Review comments

The review's comments the matcher considered, for checking `missed` verdicts.

| # | Comment |
|---|---|
| 0 | The count here is in UTF-16 units — `memberName.length()` counts code units, and `matchedPrefix` is advanced by `Character.charCount(cp)` in `TrieNode.nodeFor` — but `skipStringChars` counts *decoded code points* (its javadoc: "surrogate pairs count as a single character"). So when the skipped suffix contains a supplementary character (say a single-member record whose field is named `𝕒`, or members `a𝕒`/`b𝕓`), `remaining` comes out too large, the skip consumes the closing quote, and `consumeEndOfString` then consumes a real character. Under `-ea` that's a failed assert; in production it's a silent mis-parse. This is new behavior: the old `skipToEndOfString(int)` defaulted to `skipToEndOfString()` and ignored its argument. `memberName.codePointCount(matchedPrefix, memberName.length())` would put it in the right units. |
| 1 | The new tests cover fully-consumed names, escaped names, and escapes in the skipped suffix, but none with a supplementary (non-BMP) character in the member name — which is exactly the case where `memberName.length() - matchedPrefix` disagrees with `skipStringChars`' code-point counting. A `record R(String 😎)` case would pin that behavior down. |
