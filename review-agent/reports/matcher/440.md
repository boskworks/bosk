# Matcher validation, PR 440

Source review: `review-agent/data/eval/5fc3871d97-deepseek-deepseek-v4-flash-3c2cfb106c/440.json`. Matcher verdicts: cached (`review-agent/data/eval/5fc3871d97-deepseek-deepseek-v4-flash-3c2cfb106c/440.match.json`).

The matcher decides which review comments capture each gold finding; recall is measured from these verdicts. For each row, record in the `Your call` column whether the matcher's verdict is right: `correct` or `wrong`, plus an optional note. For a `missed` verdict, scan the review comments below to confirm none captures the finding. Recall is not trusted until these are checked.

Captured 1/4 findings.

| # | Your finding | Reviewer comment | Matcher verdict | Your call |
|---|---|---|---|---|
| 0 | Clean. I like how you did `driverSettings.collectionName()`, mirroring `driverSettings.database()`, rather than reference the static constant. | Nice test. Building the second factory from `driverSettings.toBuilder().collectionName(otherCollection)` makes the replication-group point concrete, and asserting both directions covers the symmetry. | captured | wrong. My comment is about using the "horse's mouth" value from the settings rather than the static constant; the reviewer is not talking about that. |
| 1 | The database name setting is called just `database`. Should this be called `collection`? | — | missed | correct |
| 2 | There aren't any happy path tests here; is that intentional? | — | missed | correct |
| 3 | This could be more readable with a static import. | — | missed | correct |

## Review comments

The review's comments the matcher considered, for checking `missed` verdicts.

| # | Comment |
|---|---|
| 0 | The auto-config doesn't provide a `MongoClientSettings` bean — it consumes one (the application's, or Spring's when Spring Data is present) and falls back to `spring.mongodb.uri` when neither exists. Only `MongoDriverSettings`, `BsonSerializer`, and `MongoDriverFactory` are actually created, so this list should drop `MongoClientSettings`. |
| 1 | I don't think this test can fail on the behavior it names. `MongoAutoConfiguration` builds its own `MongoClient` by applying every `MongoClientSettingsBuilderCustomizer` bean — `MongoClientFactorySupport.createMongoClient` (in spring-boot-mongodb 4.1.0) applies each customizer to a builder over the base settings — so `applied` is set whether or not the bosk factory honors the customizers. Running it against the plain `runner` with a `MongoClientSettingsBuilderCustomizer` bean (no `MongoAutoConfiguration`) would make only bosk's composition able to set the flag, and the assertion would actually mean something. |
| 2 | Nice test. Building the second factory from `driverSettings.toBuilder().collectionName(otherCollection)` makes the replication-group point concrete, and asserting both directions covers the symmetry. |
