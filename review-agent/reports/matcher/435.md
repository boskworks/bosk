# Matcher validation, PR 435

Source review: `review-agent/data/eval/5fc3871d97-deepseek-deepseek-v4-flash-3c2cfb106c/435.json`. Matcher verdicts: cached (`review-agent/data/eval/5fc3871d97-deepseek-deepseek-v4-flash-3c2cfb106c/435.match.json`).

The matcher decides which review comments capture each gold finding; recall is measured from these verdicts. For each row, record in the `Your call` column whether the matcher's verdict is right: `correct` or `wrong`, plus an optional note. For a `missed` verdict, scan the review comments below to confirm none captures the finding. Recall is not trusted until these are checked.

Captured 2/15 findings.

| # | Your finding | Reviewer comment | Matcher verdict | Your call |
|---|---|---|---|---|
| 0 | These sequences of `instanceof`s should be switches instead. | These sequences of `instanceof`s should be switches instead. | captured | correct |
| 1 | `k` is unused. Call it `_`. | `k` is unused. Call it `_`. | captured | correct |
| 2 | Does this work? I've found sometimes that logging frameworks design "getLevel" so it checks the parent level if the logger's level is not defined. If I'm right, saving this and then "restoring" it later will actually have the effect of copying the parent's level into the child. | — | missed | correct |
| 3 | `nanoTime` is overkill for things that don't require this level of precision. `getTimeMillis` will do I think. | — | missed | correct |
| 4 | All this work just to set the logger's level to TRACE?  This facility is used only when testing the log output, correct? So I'd expect the test to set whatever level it wants to test. Seems to me it's none of our business, here in the LogCapture component, to futz with the log level. | — | missed | correct |
| 5 | Neat design: `disassemble` produces a string, which is handy for the caller; the `renderXxx` methods work on a supplied `StringBuilder`, which is efficient for text generation. | — | missed | correct |
| 6 | I think I'd find all these `append` calls more readable on separate lines. | — | missed | correct |
| 7 | The JDK doesn't supply these names? | — | missed | correct |
| 8 | Can this sequence of `if instanceof` be replaced with a switch? | — | missed | correct. There is a review comment that says essentially this, but I think it's for a different line of code. |
| 9 | Ditto. | — | missed | correct |
| 10 | This means any Unicode character whatsoever, beyond 0x7f, is simply appended to the `StringBuilder`? That seems undesirable. It could be confusing things like zero-width spaces or combining diacritics, or even a mismatched surrogate. | — | missed | wrong — review comment 8 covers this: it anchors at the escape condition (`c < 0x20 \|\| c == 0x7f`, line 380), three lines above this finding's raw-append line (383), and raises the same non-ASCII-escaping concern. Matcher false-negative; corrected recall 3/15. |
| 11 | Another long sequence of `if...instanceof`. Could this be a switch? | — | missed | correct |
| 12 | Does the class file API not offer this information? | — | missed |  correct |
| 13 | I generally prefer static imports over qualified uses, especially when the qualifier is redundant. In this case, `ConstantDescs` and `CD` mean the same thing, so I'd opt for static imports. | — | missed | correct |
| 14 | These can be static imports too. | — | missed | correct |

## Review comments

The review's comments the matcher considered, for checking `missed` verdicts.

| # | Comment |
|---|---|
| 0 | These aren't all -1: `laload`/`daload` pop arrayref+index (two slots) and push a two-slot value, so their net is 0; and `lastore`/`dastore` pop four slots, so their net is -4, not -3. The array-load block should separate LALOAD/DALOAD from the one-slot loads, and the store block LASTORE/DASTORE from the one-slot stores. |
| 1 | All of these are net -1, but the long/double variants pop four slots and push two: `ladd`, `lsub`, `lmul`, `ldiv`, `lrem`, `land`, `lor`, `lxor`, `dadd`, `dsub`, `dmul`, `ddiv`, `drem` should all be -2 (`lshl`/`lshr`/`lushr` are correctly -1, since one operand is an int). And `everyOpcode_hasANetEffect` only checks that every opcode has some entry, so none of these values are actually exercised. |
| 2 | Not all conversions are net 0: the widenings `i2l`, `i2d`, `f2l`, `f2d` push a two-slot result (+1), and the narrowings `l2i`, `l2f`, `d2i`, `d2f` pop a two-slot operand (-1). |
| 3 | `lcmp`/`dcmpl`/`dcmpg` pop two long or double operands, which is four slots, and push an int: net -3, not -1. The float comparisons are correctly -1. |
| 4 | `bci` is accumulated but never read — `runningBci` below does the real bookkeeping. Looks like a leftover; the first pass only needs to number the labels. |
| 5 | These sequences of `instanceof`s should be switches instead. |
| 6 | `k` is unused. Call it `_`. |
| 7 | Confirmed the switch-entry math: per JVMS 6.5 the padding runs to the next 4-byte boundary after the opcode, so a tableswitch at bci 1 gets its default at 4 and its entries at 16/20, matching the test — and `runningBci` advancing by `sizeInBytes()` (23 here) lands the following instruction at 24. |
| 8 | `c < 0x20 \|\| c == 0x7f` leaves the C1 controls (U+0080..U+009F), Unicode format characters like U+200B, and unpaired surrogates unescaped, so they'd be emitted raw into the log. Consider `Character.isISOControl(c) \|\| Character.getType(c) == FORMAT`, and escaping lone surrogates the same way as the controls. |
| 9 | `BytecodeDisassembler.log` always computes the disassembly and doesn't swallow failures, so a disassembler bug would fail class generation whenever trace is enabled — the opposite of the care `renderDisassembly` takes below. Should the trace path be guarded the same way? |
| 10 | `renderDisassembly`, and the warn-then-throw-on-verification-failure pattern, are duplicated in `SpecCompiler`. Since codon already offers a `log` helper, maybe it should also provide a never-throwing variant that both callers can use. |
| 11 | `captureTrace` turns trace on, so the trace-level disassembly logged by `instantiate` is captured too and satisfies this assertion on its own — the test would pass even if the warn never fired. Assert on the `failed verification` message instead, so the warn path is what's actually being checked. |
