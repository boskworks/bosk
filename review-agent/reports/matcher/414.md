# Matcher validation, PR 414

Source review: `review-agent/data/eval/5fc3871d97-deepseek-deepseek-v4-flash-3c2cfb106c/414.json`. Matcher verdicts: cached (`review-agent/data/eval/5fc3871d97-deepseek-deepseek-v4-flash-3c2cfb106c/414.match.json`).

The matcher decides which review comments capture each gold finding; recall is measured from these verdicts. For each row, record in the `Your call` column whether the matcher's verdict is right: `correct` or `wrong`, plus an optional note. For a `missed` verdict, scan the review comments below to confirm none captures the finding. Recall is not trusted until these are checked.

Captured 2/2 findings.

| # | Your finding | Reviewer comment | Matcher verdict | Your call |
|---|---|---|---|---|
| 0 | I think I'd just call this `validate`. | I think I'd just call this `validate`. | captured | correct |
| 1 | This is going to forbid prefixes that end with dashes, but maybe we can live with that. | This is stricter than the bug requires: `unique("")` used to produce a perfectly good identifier ("1001"), and a trailing hyphen like `unique("foo-")` gives "foo-1001", which is valid. Only a leading hyphen actually corrupts the path segment. Rejecting the other cases is intentional? I do like that it keeps `from` and `unique` consistent, but it's a behavior change beyond the fix. | captured | correct |

## Review comments

The review's comments the matcher considered, for checking `missed` verdicts.

| # | Comment |
|---|---|
| 0 | I think I'd just call this `validate`. |
| 1 | This is stricter than the bug requires: `unique("")` used to produce a perfectly good identifier ("1001"), and a trailing hyphen like `unique("foo-")` gives "foo-1001", which is valid. Only a leading hyphen actually corrupts the path segment. Rejecting the other cases is intentional? I do like that it keeps `from` and `unique` consistent, but it's a behavior change beyond the fix. |
