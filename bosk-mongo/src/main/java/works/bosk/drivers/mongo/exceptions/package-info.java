/**
 * Exceptions that can reach the user of {@link works.bosk.drivers.mongo.MongoDriver}.
 * <p>
 * Some checked exceptions that can't reach the user are not in this package because they
 * are internal implementation details.
 */
package works.bosk.drivers.mongo.exceptions;
